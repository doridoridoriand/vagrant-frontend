---

# 必要そうなコマンド一式をインストールする
- name: Install essential packages from base repository
  apt: name={{item}} state=latest
  become: yes
  with_items:
    - wget
    - lsof
    - man
    - tree
    - dstat
    - vim
    - zsh
    - openssl
    - git
    - tmux
    - htop
    - tig

